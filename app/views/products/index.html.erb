<main>
  <h1> Products </h1>
  <div id="flex_wrapper">
    <% @products.each do |product| %>
    <article>
      <h1><%= link_to product.name, product_path(product.id)%></h1>
      <%= link_to image_tag(product.image), product_path(product.id)%>
    </article>
    <% end %>
  </div>
</main>
